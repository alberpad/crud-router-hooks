import React from "react";

export interface IProductosProps {}

const Productos = (props: IProductosProps) => {
  return (
    <div>
      <h2 className="text-center">Desde Producto</h2>
    </div>
  );
};

export default Productos;
